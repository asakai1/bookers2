<div class="container">
  <div class="row">
    <div class="form-body mx-auto col-5">
      <h1 class="text-dark bg-white mt-3">Editing Book</h1>
      <%= form_with model: @books,　local: true do |f| %>
        <div class="form-group mx-auto">
          <%= f.label :Title %><br>
          <%= f.text_field :title, class:"form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :Opinion %><br>
          <%= f.text_area :body, class:"form-control" %>
        </div>
        <%= f.submit 'Update Book', class:"btn btn-success mt-2 mb-2" %>
      <% end %>
      <%= link_to "Show", book_path(@books.id) %><%= " | " %><%= link_to "Back", book_path(@books) %>
    </div>
  </div>
</div>