<h1>編集画面</h1>
<%= form_with model: @user do |f| %>
  <label>Name</label>
  <%= f.text_field :name %>
  <label>ProfileImage</label>
  <%= f.file_field :profile_image, accept: "image/*" %>
  <label>Introcuction</label>
  <%= f.text_area :body %>
  <%= f.submit 'Update User' %>
<% end %>